# Моя инструкция Git.

## Что такое Git?

**Git** (произносится «гит») — распределённая система управления версиями. Проект был создан Линусом Торвальдсом для управления разработкой ядра Linux, первая версия выпущена 7 апреля 2005 года. На сегодняшний день его поддерживает Джунио Хамано.

 - Git - это консольная утилита, для отслеживания и ведения истории изменения файлов в проекте. Чаще всего его используют для кода, но можно и для других файлов. Например, для картинок - полезно для дизайнеров.

 *Осваивать гит проще в процессе редактирования текстовых файлов.*

## Что такое Visual Studio Code?

**Visual Studio Code** (VS Code) — текстовый редактор, разработанный Microsoft для Windows, Linux и macOS. Позиционируется как «лёгкий» редактор кода для кроссплатформенной разработки веб и облачных приложений. Включает в себя отладчик, инструменты для работы с Git, подсветку синтаксиса, и средства для рефакторинга. Имеет широкие возможности для кастомизации: пользовательские темы, сочетания клавиш и файлы конфигурации.

*Распространяется бесплатно, разрабатывается как программное обеспечение с открытым исходным кодом, но готовые сборки распространяются под проприетарной лицензией.*

## Что такое Markdown? 

**Markdown** (произносится маркда́ун) — облегчённый язык разметки, созданный с целью обозначения форматирования в простом тексте, с максимальным сохранением его читаемости человеком, и пригодный для машинного преобразования в языки для продвинутых публикаций (HTML, Rich Text и других).

*Все команды задаются при помощи написания кода в терминале.*


# Язык Markdown. <a id="anchor"></a>

### Текст:

Для того чтобы сделать **жирный текст** необходимо:

    **жирный текст** или __жирный текст__ 

Для того чтобы сделать *курсивный текст* необходимо:

    *курсивный текст* или _курсивный текст_

Для того чтобы сделать ***жирный и курсивный текст*** необходимо:

    ***жирный и курсивный текст*** или ___жирный и курсивный текст___ 

Для того чтобы ~~зачеркнуть текст~~ необходимо:

    ~~зачеркнуть текст~~ 

*Синтаксис Markdown, поддерживающий подчеркивание текста, отсутствует. На вики-странице можно использовать HTML-тег для создания подчеркнутого текста. Например,* `<u> подчеркнутый текст </u>` *возвращает подчеркнутый текст.*

### Выделение кода:

Выделение предлагаемых сегментов кода с помощью блоков выделения кода. Чтобы указать диапазон кода, заключите его тремя обратными кавычками ( ``` ) в новую строку в начале и конце блока. Чтобы указать встроенный код, заключите его в одну обратную кавычку ( ` ).

### Линия на весь экран:

---

Чтобы добавить горизонтальное линию (правило), добавьте строку, которая является рядом дефисов `---`.

### Заголовки:

Структурируйте комментарии с помощью заголовков. Они разделяют длинные комментарии на сегменты, делая их более удобными для чтения.

Чтобы указать заголовок, начните строку с символа решетки `#`. Упорядочивайте свои примечания с подзаголовками, начиная строку с дополнительными хэш-символами, например `####`. *Можно использовать до шести уровней заголовков.*

    # Выделяют заголовки

    ###### Выделяют заголовки

Элементы в нумерованных списках начинаются с числа, за которым стоит точка. В маркированных списках каждый элемент начинается с символа `-` или `*`. Начинайте каждый элемент списка с новой строки. Введите два пробела или воспользуйтесь клавишей `tab` перед разрывом строки.

1. Ненумерованные списки

    * Вложенный список

        - Дальнейший вложенный список

```
1. Ненумерованные списки

    * Вложенный список

       - Дальнейший вложенный список
```

### Цитаты:

Кавычек одинарные строки текста с `>` помощью перед текстом. Используйте много `>` символов для вложения текста в кавычки. Кавычек блоков строк текста, используя один и тот же уровень `>` между несколькими строками.

> Цитата

>>>>>>> Цитата

```
> Цитата

>>>>>>> Цитата
```

### Таблица:

Нужно поместить каждую строку таблицы в отдельной строке.

Разделение ячеек таблицы происходит с помощью символа `|`, создание заголовка с помощью `-`.

Двоеточием `:` можно выравнить текст в столбцах таблицы. 

В таблицах можно использовать некоторые из привычных форматов markdown - курсивный, жирный или зачеркнутый текст, ссылки.

```
| column1 | column2 | column3  |
|-------- |--------:|:--------:|
| Row1    | *Row2*  | ~~Row3~~ |
| Row1    | **Row2**|   Row3   |
```

| column1 | column2 | column3  |
|---------|--------:|:--------:|
| Row1    | *Row2*  | ~~Row3~~ |
| Row1    | **Row2**|   Row3   |

Чтобы сделать таблицу без заголовка нужно использовать следующий трюк:

```
|<!-- --> |<!-- --> | <!-- --> |
|-------- |--------:|:--------:|
| Row1    | *Row2*  | ~~Row3~~ |
| Row1    | **Row2**|   Row3   |
```

|<!-- --> |<!-- --> | <!-- --> |
|---------|--------:|:--------:|
| Row1    | *Row2*  | ~~Row3~~ |
| Row1    | **Row2**|   Row3   |

### Ссылка:

Для того чтобы добавить ссылку:

    [комментарий](адрес ссылки)

[GeekBrains](https://gb.ru)

### Imege:

Чтобы вставить изображение в текст, достатачно написать следущее:

    ![комментарий](путь к изображению, изображение с расширением)

![Ivan Akimov](imege/student.png)

*Можно сделать так, чтобы гит игнорировал файлы, такие как изображения, опирационные файлы системы, и др. Для этого создаем Файл* <m>.gitignore</m> *и прописываем в нём те файлы, которые не нужно отслеживать.*

### Ссылка + Imege:

Чтобы сделать изображение ссылкой:

    [![комментарий](путь к изображению, изображение с расширением)](адрес ссылки)

[![Ivan Akimov](imege/student.png)](https://gb.ru)

### Якорь:

Якорь - возвращает к месту, где он установлен:

    <a id="anchor"></a> - установка якоря

[Вверх](#anchor) - телепорт к якорю


# Команды Git.

## Начало работы.

Для начала нужно загрузить и установить Git на компьютер и VSCode *(для удобства, простоты, по желанию)*.

Git - это терминальная утилита, все взаимодействия с ней происходят через терминал. Терминал может быть представлен по разному: терминал, командная строка, bash, powershell и др. их много. *Можно работать с любым терминалом*.

Далее нужно создать папку.

*!Будут проблемы, если папка с репозиорием создана там, где в пути к ней присутствует кириллица или пробелы*.

Создать файл в терминале можно с помощью команды `tuach название файла с расширением`. 

Для того чтобы внести изменения в файле, в терминале нужно ввести `nano имя файла с расширением` 

*(Nano — консольный текстовый редактор для UNIX и Unix-подобных операционных систем).*

Или `vim имя файла с расширением` *(установлен и используется по умолчанию).*

*(Vim (сокр. от Vi Improved, произносится «вим») — свободный текстовый редактор, созданный на основе более старого vi. Ныне это мощный текстовый редактор с полной свободой настройки и автоматизации, возможными благодаря расширениям и надстройкам).*

Для того чтобы начать редактирование файла жмем `Shift` + `i`, вносим изменения. Чтобы сохранить отредактированный файл жмем `Esc`, дальше нажимаем `Shift` + `:` *(двоеточие которое рядом с клавишей* Enter *)*. Для того чтобы просто выйти пишем `q!`, для того чтобы сохранить и выйти `wq`. *(Терминал не распознаёт кириллицу).*

- Прежде чем создавать репозиторий и инициализировать Git, проверим текущую установленную версию пограммы. Для этого в терминале введём команду `git --version`.

*Если команда введена не верно или с ошибкой, Git покажет в каком месте допущена ошибка и команду можно ввести заново.*

Берём локальный каталог, который не находится под версионным контролем, и превращаем его в репозиторий.

- Для того, чтобы Git начал свою работу, необходимо его инициализировать с помощью команды `git init` *(initialization - инициализация)*.

*В указанной папке, git начнёт отслеживать изменения и создаст скрытую папку* .git

![папка игнор](Imege/hidden_folder.png)

- Текущее состояние гита, есть ли изменения которые нужно сохранить (закоммитить), показывает команда `git status`.

*Вызвать ранее введённую команду, можно стрелками на клавиатуре.*

- Создаем новый файл с расширением, редактируем, сохраняем, узнаём текущий статус.

После создания файлов добавим содержимое рабочего каталога в индекс для последующего коммита с помощью команды `git add имя файла с расширением` *(adding - добавление)*.

Чтобы все закомитить нужно ввести команду гит адд .

*Писать название целиком не обязательно: терминал дозаполнит данные автоматически с спомощью клавиши* tab.

- После добавления содержимого в файл, сохраним его, сделаем коммит с помощью команды `git commit` *(commit - совершить)*.

*Вызвав команду коммит можно указать некоторый комментарий* `git commit -m "Комментарий"`

Команда `git commit` берёт все данные, добавленные в индекс с помощью `git add`, и сохраняет их слепок во внутренней базе данных, а затем сдвигает указатель текущей ветки на этот слепок.

*Если не сделать комминтарий при выполнении `git commit`, откроется терминальный редактор vim. Далее понадобиться нажать `Shift + i`, `комментарий`, `Esc`, `Shift + :`, `wq`.*

- Чтобы посмотреть журнал сохранений, версии файла в Git, используем команду `git log`.

![имя коммита](Imege/log.png)

- Для переключения между версиями нужно указать не только интересующий коммит, но и вернуться в тот, где работаем, при помощи команды `git checkout имя коммита`.

`3c2ee8329c8960e794b355651fb985435fa6fede` - имя коммита. *Имя коммита можно писать не полностью, достатачно первых четырёх симвалов.*

- Разницу между текущим файлом и сохранённым показывает команда `git diff` *(difference - разница)*.

Нажатие клавиши `q` возвращает в исходное окно терминала.

*Имена файлов обязательно указываем с расширениями и не «теряем» пробелы.*

*Помним, что Git управляет сохраненными файлами, а не теми, что в процессе редактирования.*

*Работать с Git можно двумя способами:
Через командную строку (терминал). Через IDE.*

#### Основные команды:

`git log` — вызывать список действий и сохранений

`git init` — создать репозиторий в папке на локальной машине 

`git add` — отслеживать добавленные файлы

`git commit "комментарий"` — сохранить текущее состояние 

`git diff` — показать разницу между версиями

`git checkout` — переключиться между разными версиями

## Работа с черновиками.

- Создать ветку можно командой `git branch название новой ветки`.

*Делать это надо в папке с репозиторием.*

- Если несколько версий черновика, можно вывести ветку на экран где находимся, командой `git branch`.

- Чтобы посмтреть последний коммит на каждой из веток, выполним команду `git branch -v`.

- Чтобы посмотреть те ветки, которые уже слили с текущей, выполним команду `git branch --merged`.

- Чтобы увидеть все ветки, содержащие наработки, которые ещё не слили в текущую ветку, выполним команду `git branch --no-merged`.

- Переименовать ветку можно с помощью команды `git branch --move старое имя ветки новое имя ветки`.

- Если требуется переключиться с одной ветки на другую, вызываем команду `git checkout имя ветки`

*Когда правим текст/код в текущей ветке, автоматического слияния не происходит: *можно создавать один документ в разных версиях в разных ветках.*

- Команда `git log` покажет состояние более новых версий проекта. Но если вызвать эту команду из самой «свежей» ветки, мы не увидим исходного файла.

Допустим, черновики полностью устраивают и нужно внести изменения, чтобы информация появилась в чистовике. Для этого есть команда `git merge` *(merge - сливать)*.

Если ветка больше не нужна, ее можно удалить так `git branch -d название ветки`

*Если нароботки не были добавлены в основную ветку, гит спросить: действительно хотите удалить ветку вместе со всеми наработками? Используем опцию `-D`, как указано в подсказке.*

### Конфликт изменений.

При работе в двух ветках одновременно может возникнуть ситуация, когда в одной и другой ветке мы по-разному изменили блок текста. Если затем мы попробуем слить эти ветки, Git сообщит о конфликте и предложит выбрать, какие же изменения записать.

*Поэтому у проекта в репозитории должен быть один ответственный пользователь, наделённый правом проводить слияния и разрешать конфликты.*

### Визуализация всех веток.

Ключ `--graph` в связке с командой `log` позволяет отобразить коммиты в виде дерева `git log --graph`.

## Работа с удаленными репозиториями.

GitHub — крупнейший веб-сервис для хостинга IT-проектов и их совместной разработки.

Веб-сервис основан на системе контроля версий Git и разработан на Ruby on Rails и Erlang компанией GitHub, Inc (ранее Logical Awesome). Сервис бесплатен для проектов с открытым исходным кодом и (с 2019 года) небольших частных проектов, предоставляя им все возможности (включая SSL), а для крупных корпоративных проектов предлагаются различные платные тарифные планы.

| Git - программа | GitHub - сервис|
|:----------------|:---------------|
| git — одна из систем контроля версий | Предоставляет Microsoft
| Способ организации и поддержания версионности | Самый популярный сервис Git 
| Самая популярная система контроля версий | Много полезных функций 
|| Огромный архив различного кода|
||

Через сервис GitHub в поиске можно находить различные репозитории и скачивать их. Найденый репозиторий можно просмотреть перед скачиванием. Для того чтобы скачать репозиторий нужно:

- На сайте GitHub, когда выбрали репозиторий для скачивания, нажать кнопку `Code` (светится зеленым), и скопировать адрес (там где HTTPS).

- Далее в терминале воспользуемся командой `git clone`, которая позволяет склонировать внешний репозиторий на ПК.

- Затем необходимо перейти в скаченную папку с помощью команды `cd имя папки`.

*Скачать чужой реппозиторий можно без аккаунта в сервисе GitHub. Но если нужно направить свой репозиторий в интернет, регистрация обязательна.*

Для того чтобы залить репозиторий на сервис нужно:

- На сайте GitHub в правом верхнем углу нажать на `+` и выбрать "новый репозиторий", дать ему имя. (только латинскими буквами, без пробелов, все остальные параметры по умолчанию)

- GitHub предложит варианты. (Выбрать что нужно, и следовать инструкции):

    1. `git remonte add origin https://..` - связь локольного и удаленного репозитория.

    2. `git branch -M main` - указатель какая ветка будет оснавной.
    
    3. `git push` - позволяет отправить версию репозитория на внешний репозиторий. ТРЕБУЕТ АВТОРИЗАЦИИ на внешнем репозитории при первом пуше.

Для того чтобы направить отредактированый репозиторий обратно в удаленный репозиторий нужно воспользоваться командой `git push`.

Чтобы скачать актуальную версию с удаленного репозитория на GitHub нужно воспользоваться командой `git pull` - эта команда позволяет скачать все из текущего репозитория и автоматически сделать `merge` с нашей версией.

Поучавствовать в чужом репозитории к которому нет доступа, возможно с помощью кнопки `Fork` (вилка) - скопирует репозиторий с чужого аккаунта в свой аккаунт.

*!На GitHub принято, чтобы у каждого проекта был файл README.md - описание того, что происходи в проекте.*

*!При работе с чужими репазиториями, проводить все редактирования нужно в новой ветке.*

Далее направить измененния в интернет на GitHub. 

На GitHub появится предложение об отправке изменений владельцу репозитория который мы редактровали, зеленая кнопка `Companre & pull request`.

```
Как сделать pull request:

1. Делаем fork репозитория
2. Делаем clone своей версии репозитория
3. Создаем новую ветку и в неё вносим свои изменения
4. Фиксируем изменения (делаем коммиты)
5. Отправляем свою версию в свой GitHub
6. На сайте GitHub нажимаем кнопку pull request
```

[Сайт для тренировки git](https://learngitbranching.js.org)
